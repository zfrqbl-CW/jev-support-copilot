# Get a key from https://typesafe.ai (early access) or a supported gateway
# such as Vercel AI Gateway or Cloudflare AI.
TYPESAFE_API_KEY=

# Optional. Force mock mode even when a real key is set. Useful while
# building or screenshotting the UI without spending real API calls.
USE_MOCK=false

PORT=3000
